# pandox-ui

Workspace-level Dioxus components (Hero, Navbar, Callout, component registry). Markdown blocks are converted to RSX ahead of time through `pandox-macros` + `dx translate`, so this crate just exposes regular components without any runtime HTML shims.
